services:
  web:
    environment:
      - REDIS_HOST=myRedisCache
      - REDIS_PORT=6379
    build: .
    ports:
      - "5000:5000"
    depends_on:
      - myRedisCache

  myRedisCache:
    image: redis:latest 
    volumes:
      - redis-data:/data
    
  
volumes:
  redis-data: